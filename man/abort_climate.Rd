% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abort.R
\name{abort_climate}
\alias{abort_climate}
\title{Raise a typed error with structured metadata}
\usage{
abort_climate(message, type = "generic_error", ..., call = rlang::caller_env())
}
\arguments{
\item{message}{Human-readable error message}

\item{type}{Error type for classification. One of:
\itemize{
\item "validation_error": Data/parameter validation issues (HTTP 400)
\item "column_not_found": Missing column in dataset (HTTP 400)
\item "model_error": Statistical model failures (HTTP 422)
\item "generic_error": Unclassified errors (HTTP 500)
}}

\item{...}{Additional metadata to include in the error (e.g., column = "tmean")}

\item{call}{The call to include in the error (defaults to caller's call)}
}
\value{
Never returns; always raises an error.
}
\description{
Creates a classed condition that can be caught and inspected by the API layer.
This is the base helper - prefer using specific helpers like
\code{abort_column_not_found()} or \code{abort_validation()} when applicable.
}
\examples{
\donttest{
# Basic usage
err <- tryCatch(
  abort_climate("Something went wrong", "generic_error"),
  error = identity
)
inherits(err, "climate_error")

# With metadata
err <- tryCatch(
  abort_climate(
    "Invalid lag value",
    "validation_error",
    param = "nlag",
    value = -1,
    expected = "non-negative integer"
  ),
  error = identity
)
err$type
}

}
