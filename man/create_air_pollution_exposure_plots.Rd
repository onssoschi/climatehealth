% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_pollution.R
\name{create_air_pollution_exposure_plots}
\alias{create_air_pollution_exposure_plots}
\title{Create adaptive exposure-response plots with optimal grid layout}
\usage{
create_air_pollution_exposure_plots(
  data_with_lags,
  meta_results,
  reference_pm25,
  reference_name = "Reference",
  tlag = 2,
  vardf = 3,
  dfseas = 4,
  max_ylim = 2,
  output_file = NULL,
  plot_width_per_panel = 4,
  plot_height_per_panel = 3.5,
  res = 150,
  include_af_an = TRUE,
  var_name = "pm25",
  max_lag = 2,
  family = "quasipoisson"
)
}
\arguments{
\item{data_with_lags}{Data frame containing pre-processed data with exposure lags.
Must include columns: region, date, deaths, population, and exposure variable.}

\item{meta_results}{List. Meta-analysis results from previous analysis.
Should include overall relative risk estimate (overall_rr).}

\item{reference_pm25}{Numeric value specifying the reference PM2.5
concentration (\u03bc\u0067\u002f\u006d\u00b3) for risk calculations and plot
centering.}

\item{reference_name}{Character string describing the reference scenario
(e.g., "WHO", "National"). Defaults to "Reference".}

\item{tlag}{Integer. Maximum lag days for distributed lag models.
Defaults to 2.}

\item{vardf}{Integer. Degrees of freedom for exposure-response functions. Defaults to 3.}

\item{dfseas}{Integer. Seasonal degrees of freedom per year. Defaults to 4.}

\item{max_ylim}{Maximum Y-axis limit for relative risk. Defaults to 2.0.}

\item{output_file}{Character. Full path for output PNG file. If NULL, output path will
default to 'air_pollution_results/(reference_name)_exposure_response_plots.png'.
Default to NULL.}

\item{plot_width_per_panel}{Width (inches) per panel in output. Defaults to 4.}

\item{plot_height_per_panel}{Height (inches) per panel in output. Defaults to 3.5.}

\item{res}{Integer. Resolution (DPI) for output PNG. Defaults to 150.}

\item{include_af_an}{Logical. Whether to include AF/AN calculations in plots.
Defaults to TRUE.}

\item{var_name}{Character. Name of PM2.5 variable in dataset. Defaults to "pm25".}

\item{max_lag}{Integer. Maximum lag days. Defaults to 2.}

\item{family}{Character. Distribution family for GAM. Defaults to "quasipoisson".}
}
\value{
List containing predictions, meta model, and AF/AN values
}
\description{
Create adaptive exposure-response plots with optimal grid layout
}
