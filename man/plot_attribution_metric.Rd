% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diseases_shared.R
\name{plot_attribution_metric}
\alias{plot_attribution_metric}
\title{Plot Attributable Metrics}
\usage{
plot_attribution_metric(
  attr_data,
  level = c("country", "region", "district"),
  metrics = c("AR_Number", "AR_Fraction", "AR_per_100k"),
  filter_year = NULL,
  param_term,
  case_type,
  save_fig = FALSE,
  output_dir = NULL
)
}
\arguments{
\item{attr_data}{A data frame containing climate-health attribution results,
including columns such as \code{year}, \code{AR_Number}, \code{AR_Fraction}, \code{AR_per_100k},
and their respective confidence intervals.}

\item{level}{Character. The spatial level at which to plot the results.
Must be one of \code{"country"}, \code{"region"}, or \code{"district"}. Defaults to "district".}

\item{metrics}{Character vector. Specifies which metrics to plot.
Options include \code{"AR_Number"} (attributable number),
\code{"AR_Fraction"} (attributable fraction), and
\code{"AR_per_100k"} (attributable rate per 100,000 population).
Multiple values can be passed. Defaults to c("AR_Number", "AR_Fraction",
"AR_per_100k").}

\item{filter_year}{Optional. Integer or vector of integers specifying the year(s)
to filter the dataset. If \code{NULL}, all years are aggregated. Defaults to FALSE.}

\item{param_term}{Character. The climate variable term used in the attribution
analysis (e.g., "tmax", "rainfall"). This is used for labeling the plot titles.}

\item{case_type}{Character. The type of disease that the case column refers
to. Must be one of 'diarrhea' or 'malaria'}

\item{save_fig}{Logical. Whether to save the generated plots to file.
Defaults to FALSE.}

\item{output_dir}{Optional. Directory path to save the output plots if
\code{save_fig = TRUE}. Defaults to NULL.}
}
\value{
A list of ggplot objects (or nested lists if the level is \code{"region"} or
\code{"district"} with no \code{filter_year}). If \code{save_fig = TRUE},
the plots are also saved as PDF files in the specified \code{output_dir}.
}
\description{
This function generates plots of climate-attributable health metrics such as
the attributable number, fraction, and rate of disease cases at different spatial
levels (country, region, or district). It supports filtering by year and can
optionally save the plots as PDF files.

The function produces time series plots (if \code{filter_year} is specified) or aggregate
bar charts (if \code{filter_year} is \code{NULL}), depending on the level and user input.
}
\details{
\itemize{
\item When \code{level = "country"} and \code{filter_year = NULL}, data is aggregated by
year and plotted as a time series.
\item When \code{level = "region"} or \code{"district"} and \code{filter_year = NULL},
the function returns a list of faceted bar plots showing top-level units,
split across multiple pages if necessary.
\item When \code{filter_year} is provided, trends over time are shown for the specified
regions or districts.
}
}
\keyword{internal}
