% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diseases_shared.R
\name{plot_attribution_metric}
\alias{plot_attribution_metric}
\title{Plot Attributable Health Metrics Across Spatial and Temporal Levels}
\usage{
plot_attribution_metric(
  attr_data,
  level = "country",
  metrics = c("AR_Number", "AR_Fraction", "AR_per_100k"),
  filter_year = NULL,
  param_term,
  case_type,
  save_fig = FALSE,
  output_dir = NULL
)
}
\arguments{
\item{attr_data}{A data frame or tibble containing attribution results, typically
generated by the \code{attribution_calculation()} function. Must include relevant columns
such as \code{year}, \code{region}, \code{district}, \code{AR_Number}, \code{AR_Fraction}, and \code{AR_per_100k}.}

\item{level}{Character. The spatial level for plotting. One of \code{"country"},
\code{"region"}, or \code{"district"}. Determines the type and granularity of plots.}

\item{metrics}{Character vector specifying which metrics to plot.
Options include \code{"AR_Number"}, \code{"AR_Fraction"}, and \code{"AR_per_100k"}. Multiple metrics can be plotted.}

\item{filter_year}{Optional integer or vector of integers to restrict the plots
to specific years. Defaults to \code{NULL} (all available years are included).}

\item{param_term}{Character. The exposure variable term to evaluate (e.g., \code{"tmax"} for
maximum temperature, \code{"rainfall"} for precipitation). Used for labeling the plots.}

\item{case_type}{Character. The type of disease that the case column refers to
(e.g., \code{"malaria"} or \code{"diarrhea"}). Used in titles and y-axis labels.}

\item{save_fig}{Logical. If \code{TRUE}, saves all generated plots as PDF files
to the specified directory. Defaults to \code{FALSE}.}

\item{output_dir}{Optional string. Directory path where output PDF files will be saved
when \code{save_fig = TRUE}. If the directory does not exist, it will be created automatically.}
}
\value{
A named list of \code{ggplot} or \code{patchwork} plot objects, grouped by metric.
Each element corresponds to one metric (\code{"AR_Number"}, \code{"AR_Fraction"}, \code{"AR_per_100k"})
and may include one or more plots, depending on the level and year filters.
}
\description{
Visualizes attributable health metrics (e.g., attributable number, fraction, or rate)
derived from \code{attribution_calculation()} across different spatial scales and time periods.
The function automatically adapts plots to the selected spatial level (\code{country}, \code{region},
or \code{district}) and handles both single- and multi-year visualizations.
It supports faceted, grouped, or aggregated visualizations and can optionally
save output plots as PDF files.
}
\details{
This function produces publication-ready plots of attributable metrics:
\itemize{
\item \strong{Country level:} Time series line plots with 95\% confidence ribbons.
\item \strong{Region/District level (no filter):} Horizontal bar plots showing aggregated
metrics, grouped by administrative unit.
\item \strong{Region/District level (multi-year):} Grouped bar plots comparing metrics across years.
}

The function automatically adjusts y-axis limits, formats numeric labels with commas,
and includes optional text annotations (e.g., showing both attributable numbers and fractions).
When \code{save_fig = TRUE}, one PDF file is created per metric and spatial level, and each file
may contain multiple pages if many regions or districts are present.
}
\keyword{internal}
