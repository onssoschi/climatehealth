% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptive_stats.R
\name{run_descriptive_stats_api}
\alias{run_descriptive_stats_api}
\title{Create descriptive statistics via API-friendly inputs.}
\usage{
run_descriptive_stats_api(
  data,
  output_path,
  aggregation_column = NULL,
  population_col = NULL,
  dependent_col,
  independent_cols,
  units = NULL,
  plot_corr_matrix = TRUE,
  plot_dist = TRUE,
  plot_ma = TRUE,
  plot_na_counts = TRUE,
  plot_scatter = TRUE,
  plot_box = TRUE,
  plot_seasonal = TRUE,
  plot_regional = TRUE,
  plot_total = TRUE,
  correlation_method = "pearson",
  ma_days = NULL,
  ma_sides = 1,
  timeseries_col = NULL,
  detect_outliers = TRUE,
  calculate_rate = TRUE,
  run_id = NULL,
  create_base_dir = TRUE
)
}
\arguments{
\item{data}{The dataset for descriptive stats (list-like object or CSV path).}

\item{output_path}{Character. Base output directory.}

\item{aggregation_column}{Character. Column used to aggregate/split data by region.}

\item{population_col}{Character. The column containing the population.}

\item{dependent_col}{Character. The dependent column.}

\item{independent_cols}{Character vector. The independent columns.}

\item{units}{Named character vector. Units for each variable.}

\item{plot_corr_matrix}{Logical. Whether to plot a correlation matrix.}

\item{plot_dist}{Logical. Whether to plot histograms.}

\item{plot_ma}{Logical. Whether to plot moving averages over a timeseries.}

\item{plot_na_counts}{Logical. Whether to plot counts of NAs in each column.}

\item{plot_scatter}{Logical. Whether to plot dependent vs independent columns.}

\item{plot_box}{Logical. Whether to generate box plots for selected columns.}

\item{plot_seasonal}{Logical. Whether to plot seasonal trends.}

\item{plot_regional}{Logical. Whether to plot regional trends.}

\item{plot_total}{Logical. Whether to plot total dependent values per year.}

\item{correlation_method}{Character. Correlation method. One of 'pearson', 'spearman', 'kendall'.}

\item{ma_days}{Integer. Number of days used in moving average calculations.}

\item{ma_sides}{Integer. Number of sides used in moving average calculations (1 or 2).}

\item{timeseries_col}{Character. Timeseries column.}

\item{detect_outliers}{Logical. Whether to output an outlier table.}

\item{calculate_rate}{Logical. Whether to plot annual rates per 100k.}

\item{run_id}{Character. Optional run id.}

\item{create_base_dir}{Logical. Whether to create \code{output_path} if missing. Defaults to \code{TRUE}.}
}
\value{
A list with \code{base_output_path}, \code{run_id}, \code{run_output_path}, and \code{region_output_paths}.
}
\description{
Create descriptive statistics via API-friendly inputs.
}
\examples{
\donttest{
run_descriptive_stats_api(
  data = list(
    date = as.character(as.Date("2024-01-01") + 0:29),
    region = rep(c("A", "B"), each = 15),
    outcome = sample(1:20, 30, replace = TRUE),
    temp = rnorm(30, 25, 3)
  ),
  output_path = tempdir(),
  aggregation_column = "region",
  dependent_col = "outcome",
  independent_cols = c("temp"),
  timeseries_col = "date",
  plot_corr_matrix = TRUE
)
}

}
